# setups/claude-code-web-github/

| File | What it covers |
|---|---|
| [`README.md`](README.md) | The setup itself: environment, GitHub integration, and workflow conventions. |

No scripts here yet — this setup is provisioned by the Claude Code on the
web platform (session creation), not by a local bootstrap script. If that
changes, add a `scripts/` subdirectory with its own `INDEX.md`.
