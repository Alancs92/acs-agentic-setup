# scripts/

Shared automation that isn't specific to a single setup — common install
steps, dotfile sync, cross-setup bootstrap helpers. Scripts that only make
sense for one setup belong inside that setup's own directory
(`setups/<slug>/scripts/`) instead, so the setup stays self-contained.

| Script | Purpose |
|---|---|
| _(none yet)_ | |
